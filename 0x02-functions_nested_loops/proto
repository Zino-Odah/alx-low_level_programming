#include "main.h"

/**
 * print_sign - prints the sign of a number
 *
 * @n: value to be checked
 *
 * Return: 1 if n is + and > 0, 0 if n is 0 and = 0, -1 if n is - and < 0
 */

int print_sign(int n)
